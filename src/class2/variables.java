package class2;

public class variables {
    public static void main(String[] args) {
        /*below line is reserving a box in the computers memory.
        we are colling it box1 if we need the information that we are storing inside this box .....
         */
        int box1=10;
        System.out.println(box1); // bring the contents of box1 and print them of the console
        /* to store whole numbers(Numbres without decimals points)   we have 4 different types of boxes.
         */
        byte box2=127; // range for bytr is from -128 to 127 if we need to store langer numbers we should try short
        byte box3=-128;
        short box4=32767;
        int biggerbox=178322453; // most common type of box
        long maxbox=12837124472346L;
        short mybox=10000;
        System.out.println(mybox);



    }
}
